# Exam Flow — Backend

Quick start (backend):

1. Copy `.env.example` to `.env` and update values.
2. Install dependencies:

```bash
cd backend
npm install
```

3. Start dev server:

```bash
npm run dev
```

Server entry: `src/index.js`. The app mounts routes under `/api`.
